name: wolves-bot

on:
  # Runs at :17 each hour. Top-of-the-hour (:00) and round 5/10-min marks are
  # the most congested, so GitHub is most likely to delay or drop runs then --
  # an odd minute is more reliable. Running early in the hour also leaves plenty
  # of delay headroom before the clock hour rolls over. bot.py decides whether
  # THIS hour is one of today's posting slots, so most runs do nothing.
  schedule:
    - cron: "17 * * * *"
  # Lets you trigger a run by hand from the Actions tab (handy for testing).
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Plan only, don't actually post"
        type: boolean
        default: false
      force:
        description: "Post immediately, ignoring the schedule (for testing)"
        type: boolean
        default: false

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r wolves-bot/requirements.txt

      - name: Run the bot
        working-directory: wolves-bot
        env:
          # X (Twitter)
          CONSUMER_KEY: ${{ secrets.CONSUMER_KEY }}
          CONSUMER_SECRET: ${{ secrets.CONSUMER_SECRET }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          ACCESS_TOKEN_SECRET: ${{ secrets.ACCESS_TOKEN_SECRET }}
          # Mastodon (add these secrets once your account is ready)
          MASTODON_API_BASE_URL: ${{ secrets.MASTODON_API_BASE_URL }}
          MASTODON_ACCESS_TOKEN: ${{ secrets.MASTODON_ACCESS_TOKEN }}
          # Bluesky (add these secrets once your account is ready)
          BLUESKY_HANDLE: ${{ secrets.BLUESKY_HANDLE }}
          BLUESKY_APP_PASSWORD: ${{ secrets.BLUESKY_APP_PASSWORD }}
          DRY_RUN: ${{ inputs.dry_run && '1' || '0' }}
          FORCE_POST: ${{ inputs.force && '1' || '0' }}
        run: python bot.py
